[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "isp-assignment"
version = "0.1.0"
description = "Image signal processing assignment package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["awb_cst", "debayering"]
